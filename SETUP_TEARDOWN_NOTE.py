# [Teardown]
Tear down will execute after executing the test case

# [Setup]
setup is something that is executed before a test case 

# Suite Setup    :--
It will execute before executing the all test cases 

# Suite Teardown:-  
It will execute after executing the all test cases

# Test Setup    :--
It will execute before executing every test case if test case doesn’t have any setup section.

# Test Teardown:-
It will execute after executing every test case if test case doesn’t have any teardown section.
